# Project lives on an NTFS (ntfs3) mount where pnpm's default symlinked store
# breaks Turbopack's resolver/watcher on this machine. hoisted = flat, real
# node_modules (like npm) so Turbopack can traverse it. Harmless on other FSes.
nodeLinker: hoisted

allowBuilds:
  sharp: false
  unrs-resolver: false
